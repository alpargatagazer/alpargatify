[Definition]
# Match "Failed password for" in SFTP container logs (atmoz/sftp)
# Example log: sftp  | Failed password for invalid user admin from 192.168.1.10 port 1234 ssh2
failregex = ^.*Failed password for .* from <HOST> port .* ssh2$

ignoreregex =
