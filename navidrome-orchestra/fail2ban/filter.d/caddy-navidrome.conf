[Definition]
# Match Navidrome-specific auth endpoints with failed status codes
# Targets: /, /rest/, /auth, /api/ endpoints
failregex = ^.*"remote_ip":"<HOST>".*"uri":"(\/|\/rest\/|\/auth|\/api\/)".*"status":(401|403).*$

ignoreregex =
